{
  "name": "src",
  "version": "1.0.0",
  "description": "Creating the Hello World API, followed by integration for testing using Playwright and GitHub Actions.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js &",
    "test": "playwright test &&  allure generate test-results --clean && allure open &"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@playwright/test": "^1.40.1",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "playwright": "^1.40.1"
  },
  "devDependencies": {
    "allure-playwright": "^2.9.2"
  }
}
